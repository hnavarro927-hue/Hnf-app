# Copiar a .env.development.local según necesidad (no commitear secretos).
# Desarrollo: vacío → rutas relativas + proxy Vite (vite.config.js).
# Producción: usar .env.production o variables en CI (Vercel).

# VITE_API_BASE_URL=https://hnf-app.onrender.com

# Solo dev: destino del proxy (opcional)
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:4000
